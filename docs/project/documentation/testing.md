# Test conventions

Test conventions verified in the product codebase, recorded once so
`$test-story` does not re-derive them from scratch on every story. Record only
a convention actually observed in the code; do not guess or aspire to a
convention that is not yet true.

No product code has been reviewed yet. This page stays empty until the first
story that inspects the test suite populates the relevant section below.

## Frameworks and commands

## Test file layout and naming

## Fixtures and mocking

## Coverage expectations

## Continuous checks

Each entry should cite the file or files it was verified against. Superseded
guidance is replaced, not accumulated — keep one current statement per topic.
