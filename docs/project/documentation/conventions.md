# Coding conventions

Conventions verified in the product codebase, recorded once so `$design-story`
and `$develop-story` do not re-derive them from scratch on every story. Record
only a convention actually observed in the code or explicitly decided by the
user; do not guess or aspire to a convention that is not yet true.

No product code has been reviewed yet. This page stays empty until the first
story that inspects the source populates the relevant section below.

## Language and tooling

## Naming

## Module and dependency boundaries

## Error handling

## Formatting and linting

## Dependency policy

Each entry should cite the file or files it was verified against. Superseded
guidance is replaced, not accumulated — keep one current statement per topic.
