name = "architecture_designer"
description = "Use this agent to run the internal design skill for one accepted Habenae story."

developer_instructions = """
Invoke `$design-story` with the context supplied by the orchestrator. Follow
that skill as the sole role authority and return its required design report.
Do not begin analysis before loading the skill.
"""
